# 변경 이력

## v5.1 (2026-08) — 수익성 점검: 체결 정확도·상태 정합성

리밸런싱 모드로 전환한 뒤 **백테스트가 한 건도 없던 상태**를 해소하고,
실제로 수익을 갉아먹던 체결·상태 버그를 수정.

### 신규 — backtest_rebalance.py
- 전략 5종 백테스트. 로직 재구현 없이 `_compute_dual`/`_compute_vaa`를 그대로 호출해
  실제 봇 성과와 동일함을 보장
- look-ahead 차단(판단은 전일 종가까지, 체결은 당일 시가+슬리피지),
  수수료·정수 주수·현금 잔여 반영
- CAGR/MDD/Sharpe/Calmar/연도별 수익률/거래비용, KOSPI200 보유 대비 비교
- `--exec-at open|close`로 개장 직후(09:05) 집행 비용을 추정이 아닌 **실측**

### 신규 — 배포 점검 (scanner/doctor.py)
이 봇에서 돈이 새는 대부분은 코드 버그가 아니라 '설정이 조용히 잘못된 상태'다.
예전 .env에 REBALANCE_TIME=09:05가 남아 매달 스프레드를 물거나, 자본이 최소요구액에
미달해 목표 종목을 못 담거나, 과세 비중이 높은 줄 모른 채 운용되는 식이다.
`python -m scanner.doctor`가 이를 한 번에 점검하고 조치 방법까지 출력한다.
- 설정: 전략 유효성·체결 시각·매수 버퍼·대시보드 토큰·자동매매/계좌 정합·텔레그램·리뷰 스케줄
- 실계좌: 시세 조회 가능 여부·자본 적정성(최소 필요금액 대비)·과세 노출
- 조치 필요 시 종료코드 1 (배포 스크립트에 연결 가능), `--no-net`으로 오프라인 점검
- 점검 하나가 실패해도 나머지는 계속 수행

### 신규 — 세후 수익 가시화 (가장 큰 비용)
전략은 세전 모멘텀만 보므로 세금이 어디에도 드러나지 않았다. 일반 위탁계좌 기준
국내 주식형 ETF는 매매차익 비과세지만, 해외지수·원자재·채권 ETF는 15.4% 과세다.
과세 비중 67% 구성에서 세전 10% 수익이면 약 1.0%p가 세금 — 이 봇이 절감한
스프레드·현금 드래그를 합친 것보다 크다.
- `tax_profile()`: 보유 금액 비중 기준 과세비중·실효세율·세부담 추정
- 매월 전략 리뷰에 표시 + ISA·연금저축 안내 (계좌 구조 문제라 코드로는 해결 불가)
- `build_review()`는 순수 함수로 유지(세금 조회는 호출부에서) — 리포트 생성이
  KIS 장애에 묶이지 않도록

### 변경 — 리밸런싱 기본 체결 시각 09:05 → 10:00
백테스트가 아니라 시장 구조로 결정되는 부분. 국내 ETF의 유동성공급자(LP)는 장 개시 후
5분간 호가 제출 의무가 면제되어 09:00~09:05 구간은 스프레드가 크게 벌어지고, 09:05
직후에도 호가가 채워지는 중이다. 이 봇은 시장가(ORD_DVSN=01) 주문이라 그 스프레드를
그대로 부담하며, 거래대금이 적은 ETF(나스닥100·금·반도체)일수록 손실이 크다.
월 1회 주문이라 몇 시간 늦춰도 전략 성격은 불변인 반면 스프레드 손실은 매달 확정적이므로
유동성이 회복된 10:00을 기본값으로 한다. 09:00~09:10 설정 시 기동 경고 발송.

### 신규 — kr_ensemble (멀티전략 앙상블)
표본이 짧아 '1등 전략'을 확신할 수 없는 문제에 대한 구조적 답. 듀얼모멘텀 3종의
목표 비중을 평균해 여러 전략이 합의한 자산에 비중이 실리게 한다(3/3 33.3% ·
2/3 22.2% · 1/3 11.1%). 합성시장 검증에서 CAGR은 구성원 최악보다 우수, MDD는
구성원 최악(-3.7%) 대비 크게 개선(-1.1%). 기본값은 유지 — 보유 종목이 늘어
소액 계좌에선 정수 주수 드래그가 커지므로 월간 리뷰 비교 후 선택하도록 둠.
`_dispatch()`로 전략 타입별 진입점을 단일화해 백테스트도 동일 로직을 검증한다.

### 신규 — 전략 리뷰 자동화 (job_strategy_review.py)
전략 선택이 수익에 가장 크게 작용하는데 백테스트를 사람이 기억해 수동 실행해야 하면
사실상 검증 없이 운용된다. 이 의존성을 제거했다.
- **배포 직후 1회 자동 실행** (리뷰 이력 없을 때만 — 첫 리포트까지 한 달 대기 제거)
- 매월 25일 18:00 정기 실행 + 텔레그램 `/review` 즉시 실행
- 5개 전략 + KOSPI200 보유를 CAGR/MDD/Calmar로 비교, 실제 평가금액을 시드로 사용
- **자동 전환은 하지 않음** — 최근 성과 추종은 수익을 갉아먹으므로 보고만 하고
  교체는 사람이 결정. 판정도 보수적(표본 3년 미만 보류, Calmar 격차 40% 미만은
  유지 권장, 벤치마크 열위 시 명시 경고)

### 수정 — 치명적: 데이터 장애가 전량 청산을 유발
FDR 시세 조회 실패 시 `_compute_dual`이 안전자산 100%를 반환해, 리밸런싱일에 API가
죽어 있으면 **보유 전량을 매도하고 국고채로 갈아탔다**. 전략 신호가 아니라 API 장애로
시장을 이탈하는 것이며, 재진입 시 왕복 비용과 반등 미참여 손실이 발생한다.
- 원인: '데이터 없음'과 '전 자산 약세'가 모두 `{safe:100}`으로 귀결돼 구분 불가
- 조치: 상위 N개를 고를 만큼 모멘텀을 못 구하면 판단 불가(`{}`)로 처리 →
  `preview_rebalance`가 `actionable=False` 반환 → `execute_rebalance`가 주문·저장 없이
  중단하고 텔레그램 경고. 대시보드도 503 안내(전량 매도 표 미표시).
- 데이터가 충분한데 전 자산이 약세인 **진짜 방어 신호는 그대로 동작**한다.

### 수정 — 실제 손실 요인
| 문제 | 영향 | 조치 |
|------|------|------|
| 주문 결과를 무시하고 목표를 그대로 저장 | 매수 실패해도 보유로 기록 → 평가금액 과대·차월 주문 오산·실현손익 왜곡 | 주문 후 KIS 잔고 재조회로 실제 수량·매입평균가 동기화 |
| 수량은 FDR 전일 종가, 평가는 실시간가 | 갭 발생 시 비중 왜곡·주문가능금액 초과로 매수 전량 실패 | 실시간가로 사이징 통일(+중복 시세 호출 제거) |
| 정수 내림 잔돈 방치 | 매달 유휴 현금 드래그(소액 계좌일수록 큼) | 예산 내 미달 슬롯에 잔돈 스윕 (예: 3.0%→1.0%) |
| 데이터 조회창 430일 = 252일 룩백 대비 여유 36일 | 휴장·결손 시 12개월 모멘텀이 조용히 빠진 채 매매 | 550일로 확대 + 룩백 미확보 경고 |

### 품질
- 테스트 130 → 248 (백테스트 엔진 무결성 13건, 리컨실리에이션 4건, 잔돈 스윕 6건,
  실시간가 사이징 2건, 데이터 장애 가드 7건, 전략 리뷰 판정 14건, 앙상블 7건, 체결시각 검증 11건,
  과세 구조 18건, 기동 리뷰 8건, 배포 점검 21건)
- README에 전략 선택 절차(백테스트 해석 순서)와 수익 직결 설정 정리

## v5.1 (2026-06~07) — 월간 리밸런싱 모드 전환

### 전략
- `STRATEGY_MODE=rebalance` 신설 및 기본값 지정 — 눌림목은 `breakout` 레거시 모드로 유지
- SeedNGrow KR 5종 전략 포팅 (`scanner/strategy_rebalance.py`):
  듀얼모멘텀 4종(자산배분·kr_gem·성장주·주도주) + VAA 카나리아(13612W)
- 매월 첫 거래일 `REBALANCE_TIME` 자동 실행 + 대시보드 수동 실행
- 장마감 15:40 평가금액 스냅샷, 입출금 보정 TWR, KOSPI200 알파 비교
- KIS 잔고조회 간헐 실패 시 0원 스냅샷 방지 가드, 매도 실현손익 전략 무관 계산

### 대시보드
- Robinhood 스타일 리디자인 (Satoshi 폰트, #00C805/#F0463A 손익 색, 히어로 위계)
- 포트폴리오·리밸런싱 미리보기/실행·리밸런싱 내역·입출금 기록·전략 변경 UI
- `DASHBOARD_TOKEN` 필수화(20자 이상) — 하드코딩 기본 토큰 제거
- UI 전수 점검 2회 (`UI_CHECKLIST.md`) — 모달 폭·하드코딩·죽은 CSS 등 수정

### 품질
- 리밸런싱 코어 테스트 신설 (전략 엔진 23건 + 잡 12건) — 총 130개 통과
- requirements.txt에 fastapi·uvicorn 보완, 개인 런타임 데이터 .gitignore 등록
- README·.env.example 리밸런싱 기준으로 갱신

## v5.1 — 신호 빈도 복원 (필터 재조정)

### 문제 진단
v4.6에서 5개 필터를 동시에 강화해 신호 빈도가 백테스트 대비 사실상 0에 수렴.

| 병목 원인 | 영향 |
|----------|------|
| `price_range_pct=0.70` (신규, 미검증) | 후보의 ~70% 탈락 — 최대 병목 |
| `pullback_shape=0.20` | 전체 캔들 10~15%만 통과 (도지형 기준 과도) |
| `pullback_vol=0.70` | 거래량 소진 기준 엄격 |
| `bo_lookback=3` (하드코딩) | 기준봉 탐색 3일 이내로 제한 |
| `min_buy_pressure=110` | 눌림목(저거래량) 구간에서 110 달성이 구조적으로 어려움 |

### 파라미터 변경
| 파라미터 | 이전 | 이후 | 근거 |
|---------|------|------|------|
| `price_range_pct` | 0.70 | **0.55** | 150일 상위 30% → 45% — 신호 병목 해소 |
| `pullback_shape` | 0.20 | **0.30** | 도지 기준 완화 — 조정 캔들 포괄 |
| `pullback_vol` | 0.70 | **0.80** | 거래량 감소 기준 현실화 |
| `bo_lookback` | 3 | **5** | 기준봉 탐색 기간 3일 → 5일 확장 |
| `min_buy_pressure` | 110 | **100** | 눌림목과 고강도 매수세 상충 해소 |

### 버그 수정
- **`bo_lookback` 하드코딩 버그**: `job_screener.py`·`stock_scanner_v4.6.py` 두 파일에서  
  `for lookback in [1, 2, 3]` 하드코딩 → `range(1, STRATEGY["bo_lookback"] + 1)` 수정.  
  (백테스트 `backtest_v2.py`는 이미 `range(1, cfg["bo_lookback"] + 1)` 사용 중이었음)

---

## v5.0 — 모듈화 리팩토링

### 신규
- `scanner/` 패키지: 2,202줄 단일 파일 → 19개 모듈로 분리
- `main.py`: 스케줄러 전용 진입점 (모든 로직은 scanner/ 내부)
- `tests/`: pytest 62개 테스트 (analytics·calendar·positions·config)
- `conftest.py`: 프로젝트 루트 pytest 설정

### 모듈 역할 분리
| 모듈 | 분리 내용 |
|------|----------|
| `scanner/config.py` | STRATEGY + 환경변수 단일 출처 |
| `scanner/state.py` | 전역 Lock·Flag·캐시 집중 관리 |
| `scanner/analytics.py` | 순수 함수 (RSI·신호점수·KOSPI 필터) |
| `scanner/kis.py` | KIS API 클라이언트 |
| `scanner/job_*.py` | 스케줄 작업별 독립 모듈 |

---

## v4.6 — KIS 자동매매 연동

### 신규
- **자동매매 엔진**: `AUTO_TRADE=true` 시 2차 검증 통과 → 시장가 매수 자동 실행
- **자동 청산**: TP/SL/TRAIL_SL/HARD_SL/EXPIRE 도달 시 시장가 매도 자동 실행
- **`/autotrade on|off`** 명령어: 런타임 자동매매 토글
- **`KIS_ACCOUNT_NO`** 환경변수: 계좌번호 설정
- **`TRADE_AMOUNT_PER_STOCK`** 환경변수: 종목당 최대 투자금액
- **신호점수 비례 사이징**: 40~59점 ×0.6 / 60~79점 ×1.0 / 80점↑ ×1.5
- **09:10 갭오픈 SL 체크** (`job_morning_sl_check`): 진입 익일 갭손실 조기 차단
- **14:50 KIS 토큰 선발급** (`job_preload_kis_token`): 15:20 주문 지연 방지
- **EXPIRE 사후 PnL 추적** (`post_expire_pnl`): 만료 5영업일 후 가격 자동 기록

### 파라미터 변경
| 파라미터 | 이전 | 이후 | 근거 |
|---------|------|------|------|
| `tp_pct` | 0.13 | **0.07** | 백테스트 TP max +7.46% — 13%는 TP 미발동으로 PF 붕괴 |
| `tp1_pct` | 0.04 | **0.00** | TP=7%에서 분할 익절 시 PF 하락 |
| `sl_limit` | 0.10 | **0.04** | R:R 2.03:1 유지, SL 거리 4% 초과 탈락 |
| `hard_stop_pct` | 0.07 | **0.05** | 09:10 갭체크 추가로 7% 불필요 |
| `trail_activate_pct` | 없음 | **0.03** | 즉시 트레일링 → 손실 확대 문제 해소 |
| `bo_body_pct` | 0.07 | **0.09** | 신호 품질 향상 |
| `bo_vol_ratio` | 2.5 | **3.0** | 신호 품질 향상 |
| `rsi_min` | 30 | **45** | RSI<45는 하락 추세 가능성 |
| `pullback_vol` | 1.0 | **0.7** | 거래량 더 확실히 소진된 눌림목만 허용 |
| `min_buy_pressure` | 100 | **110** | 체결강도 기준 강화 |
| `max_hold_days` | 10 | **7** | EXPIRE 평균 7.0일, 10일 불필요 |

### 버그 수정
- **`sl_limit` 하드코딩 버그**: `job_second_screen()`에서 `0.10` 하드코딩 → `STRATEGY["sl_limit"]` 참조
- **트레일링 즉시 발동 버그**: `hwm=entry`로 초기화되어 `trail_sl=entry×0.95` 즉시 SL 역할 → `trail_activate_pct` 도입으로 해소
- **섹터 경고 미차단 버그**: 섹터 집중 시 텔레그램 경고만 발송 → 신호점수 하위 종목 자동 제외로 강화
- **`_first_screen_cache` 레이스 컨디션**: 14:30 write / 15:20 read 동시 접근 무보호 → `_cache_lock` 추가

---

## v4.5 — 운영 안정성 강화

- **logging 도입**: `print()` → `logging.info/warning/error`, RotatingFileHandler
- **스레드 안전성**: `_pause_signals`, `_tg_update_offset`에 `threading.Lock()` 적용
- **`is_market_closed()`**: 장 시간 외 시간대 처리 추가
- **FDR 지수 백오프**: 1s → 2s → 4s 재시도
- **Graceful shutdown**: SIGINT/SIGTERM 수신 시 스케줄 루프 정상 종료
- **`_file_lock`**: positions.json 읽기/쓰기 Lock 적용
- **FDR 호출 간격**: 0.05s → 0.1s (IP 차단 위험 감소)

---

## v4.4 — 양방향 텔레그램 + 신호 품질

- **텔레그램 Long Polling**: `/positions`, `/report`, `/pause`, `/resume` 명령어
- **체결강도 필터**: KIS API 실시간 확인 (≥100)
- **신호 점수화**: 0~100점 종합 평가
- **Phase 3 성과 추적**: 주간 리포트, 드리프트 감지, Sharpe 비율
- **KOSPI MA20 필터**: 시장 약세 시 스크리닝 억제

---

## v4.3 — 포지션 관리

- `positions.json`: 포지션 파일 영속성
- TP/SL 장중 모니터링 (10:00 / 13:00)
- 트레일링 스탑: HWM × (1 - trail_pct)
- EXPIRE: 기간 만료 포지션 정리 알람

---

## v4.2 — 기본 스윙 눌림목 탐지

- 1차 스크리닝 (14:30): FDR 전종목 스캔
- 2차 검증 (15:20): KIS 실시간 재검증
- 텔레그램 단방향 알림 발송
